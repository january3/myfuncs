#' Save history and data image
#'
#' Save history and data image
#'
#' Save history and data image. Same as \code{savehistory(); save.image()}.
#'@export
zz <- function() { savehistory() ; save.image() }
